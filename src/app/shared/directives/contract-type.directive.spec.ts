import { ContractTypeDirective } from './contract-type.directive';

describe('ContractTypeDirective', () => {
  it('should create an instance', () => {
    const directive = new ContractTypeDirective();
    expect(directive).toBeTruthy();
  });
});
