import { IssuePriorityDirective } from './issue-priority.directive';

describe('IssuePriorityDirective', () => {
  it('should create an instance', () => {
    const directive = new IssuePriorityDirective();
    expect(directive).toBeTruthy();
  });
});
