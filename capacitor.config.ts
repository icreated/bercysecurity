import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bercysecurity.bercy',
  appName: 'bercy',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
